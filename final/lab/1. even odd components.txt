n = -10:10;
N = length(n);

y = zeros(1,N);
for i=1:N
	if(n(i)<0)	y(i)=0;
	else	y(i)=1;
	end
end

ye = zeros(1,N);
yo = zeros(1,N);

for i=1:N
	idx = N-i+1;
	ye(i) = (y(i) + y(idx)) / 2;
	yo(i) = (y(i) - y(idx)) / 2;
end

figure;
grid on;
stem(n,ye);
stem(n,yo);
