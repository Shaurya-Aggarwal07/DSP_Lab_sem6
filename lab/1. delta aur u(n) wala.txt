% delta wala
n = -5:5;

y = zeros(1,length(n));
y(n==-2) = 2;
y(n==4) = 1;

figure;
stem(n,y);
grid on;
% --------------

% u(n) wala

n = 0:20;
y = zeros(1,length(n));

for i=1:10
	y(i) = n(i);
end

for j=11:20
	y(j) = 10*exp(-0.3*(n(i)-10));
end

figure;
stem(n,f);
grid on;
% ----------------